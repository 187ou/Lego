# LEGO-Mate 智能拼搭助手 —— PRD（完整版）

**版本**：V2.2 | **状态**：可开发 | **目标**：简历级项目


## 一、产品定位

**一句话描述**：基于多模态Agent的乐高拼搭智能副驾，用户通过**聊天+拍照**获得缺件替代、说明书检索、拼搭验收及**挫折期心理激励**一站式服务。

**核心价值**：将拼搭中断时间从**天级→分钟级**，同时通过情绪感知干预，预估将用户“弃坑率”降低40%。


## 二、用户场景（6个核心故事）

| # | 用户说（或行为表现） | Agent做 |
| :--- | :--- | :--- |
| 1 | 拍图+“少了个红色2x4砖” | 视觉解析→查图谱→推荐替代方案 |
| 2 | “第35步怎么拼？” | RAG检索→返回说明书图文 |
| 3 | 拍成品图+“帮我看下对么” | CLIP对比→相似度评分→通过/驳回 |
| 4 | **行为触发**：同一拼搭步骤连续提问≥3次，或挂起超3分钟无操作 | 主动弹出鼓励提示：“这一步确实有点绕，试试先把左下角的卡扣对齐”，并附带一句该步骤的“避坑小贴士”（从论坛语料RAG抽取）。 |
| 5 | 用户输入：“烦死了”“不想拼了”“太难了”等负面情绪词 | 启动**心流急救**：推送该套装的官方成品3D渲染图（视觉正反馈刺激），并附带进度条：“你已经完成68%的颗粒，超过同期53%的玩家！” |
| 6 | CLIP验真结果为`驳回`（拼装错误） | **先肯定后纠正**：“整体配色很还原！不过车头大灯方向装反了，只拆2颗砖就能修复，我帮你高亮圈出来了。” |


## 三、功能模块（6大核心）

| 模块 | 功能 | 优先级 |
| :--- | :--- | :--- |
| **对话中枢** | LangGraph ReAct Agent，多轮记忆（Redis缓存10轮），意图自动路由，模糊反问 | P0 |
| **视觉解析** | Qwen-VL识别图片→输出结构化JSON（零件/颜色/步骤号/置信度），置信度<70%拒答 | P0 |
| **图谱推理** | Neo4j存储零件替代关系，缺件时精确查询替代方案（100%准确，零幻觉） | P0 |
| **RAG检索** | ChromaDB + BM25/向量混合检索，召回说明书对应步骤图文 | P1 |
| **验真闭环** | CLIP对比用户成品图 vs 官方渲染图，三级决策（通过/存疑/驳回） | P1 |
| **心理感知与激励引擎** | ① **挫折计数器**：在LangGraph State中维护`frustration_score`，监测同一节点重试次数（>2次）、挂起时长（>3min）、负面关键词命中（烦/难/晕/不拼了）；② **共情话术库**：按难度等级（零件数>50为高难）匹配鼓励语/冷知识/进度百分比；③ **进度心电图**：基于已拼颗粒数/总颗粒数+用户历史速率，实时预估剩余时间并展示。 | P1 |


## 四、状态流转（LangGraph）

```
待解析 → 进度确认 → 缺件待补/结构纠偏 
                     ↓ (挂起超时/重复提问/情绪词命中)
                【心理安抚节点】← 非阻塞异步触发（不打断主流程，仅流式输出文本/图片）
                     ↓
              等待用户操作（挂起）→ 二次验收 → 已归档
```

**关键设计**：
- 所有涉及“执行”的节点（如创建补件工单）强制Human-in-the-loop挂起，等待用户确认。
- 心理安抚节点为**旁路设计**，仅修改State中的`frustration_score`和输出消息队列，不改变主状态机的挂起/归档决策，保证核心逻辑纯净。


## 五、异常处理策略

| 场景 | 处理方式 |
| :--- | :--- |
| Qwen-VL 置信度 <70% | 返回提示：“图片不够清晰，请重新拍摄或描述零件名称”，提供文本输入兜底 |
| Neo4j 查不到替代方案 | 返回相似零件列表（基于颜色/尺寸模糊匹配），并标注“需人工确认” |
| RAG 检索无匹配步骤 | 反问用户确认步骤号，或返回前后步骤供参考 |
| CLIP 相似度 60%-80%（存疑）| 返回局部放大对比，提示用户检查关键特征点 |
| 用户输入完全无法解析 | 两轮澄清后仍失败，转人工客服兜底 |
| **同一节点挂起超3分钟无用户输入** | **主动推送一条“闯关提示”+ 当前进度百分比，降低用户认知负荷。** |
| **用户输入命中负面情绪词（如“不拼了”）** | **优先推送该套装成品高清渲染图（视觉正反馈），再询问“需要我帮你拆解到上一步吗？”** |


## 六、技术栈清单

| 层级 | 技术 |
| :--- | :--- |
| Agent框架 | LangGraph + LangChain |
| 多模态模型 | Qwen-VL-Plus / GPT-4o |
| 图数据库 | Neo4j（Docker） |
| 向量数据库 | ChromaDB |
| 嵌入模型 | BGE-M3 |
| 视觉对比 | CLIP（ViT-B/32） |
| 缓存 | Redis |
| 消息通知 | 飞书/钉钉 Webhook |
| 部署 | Docker Compose |


## 七、数据来源

| 数据类型 | 来源 | 处理方式 |
| :--- | :--- | :--- |
| 零件清单 & BOM表 | Rebrickable.com 下载CSV | 直接导入 Neo4j |
| 说明书 | Rebrickable 提供的官方说明书 PDF 链接（或 Brickset 数据库） | PDF切片 → ChromaDB 索引 |
| 替代关系 | CSV导入（Rebrickable替代数据）+ LLM从论坛抽取 | LLM抽取后**人工校验**，确保100%准确 |
| 官方渲染图 | Rebrickable / BrickLink 官方零件渲染图 | 预下载作为CLIP对比基准 |
| **共情话术与避坑贴士** | **Brickset论坛/Reddit r/lego 高热评论（爬取）+ 人工精选20条冷知识/鼓励语** | **构建JSON话术库，按步骤难度和情绪类型索引** |

> **说明**：乐高官网不提供公开说明书 PDF API，本项目通过 Rebrickable 提供的第三方链接获取。


## 八、开发排期（10天）

| 阶段 | 内容 | 交付物 |
| :--- | :--- | :--- |
| D1 | Docker环境搭建（Neo4j + Redis）+ LangGraph 对话骨架 | 能闲聊的机器人 |
| D2 | LangGraph 接入一个 Mock 工具，验证 ReAct 链路 | 工具调用跑通 |
| D3-4 | 接入Qwen-VL，实现图片→JSON解析 | 上传图片返回结构化数据 |
| D5-6 | Neo4j导入数据 + **在LangGraph State中增加`frustration_score`计数器及情绪词正则检测模块（约1小时）**，封装为LangChain Tool | 输入零件名返回替代品 + **基础情绪感知就绪** |
| D7 | RAG检索（PDF切片+ChromaDB） | 输入问题返回说明书图文 |
| D8 | CLIP验真 + LangGraph状态机打通（含心理安抚旁路节点） | 全流程闭环跑通 |
| D9 | 飞书Webhook接入 + **填充共情话术库JSON（预设20条）** + **录制“拼错→沮丧→AI安抚+圈图纠正”完整演示视频** | 通知能发到手机 + **最打动面试官的Demo片段** |
| D10 | README + PPT | 项目收尾 |

**风险预留**：D7-D8 为关键路径，若延期可砍 RAG/CLIP 为非核心版本（返回文本而非图文）；心理激励模块为纯旁路逻辑，不影响主流程稳定性。


## 九、量化目标（简历数据）

| 指标 | 目标值 | 验收方式 |
| :--- | :--- | :--- |
| 替代方案准确率 | 100%（图谱硬规则） | 50个测试用例，人工校验 |
| 工具调用准确率 | ≥95% | 30条预设对话，统计路由正确率 |
| 成品验收判定准确率 | ≥90% | 20张成品图（含10张错误样本） |
| 端到端响应时间 | ≤2.5秒 | P95 耗时，不含大模型推理等待 |
| 单次对话成本 | 较纯 GPT-4o 方案降低60% | 对比基准：纯 GPT-4o 多轮对话（平均8轮） vs 当前方案（简单问题不调大模型，图谱/RAG直接返回） |
| **情绪干预触发准确率** | **≥80%（不误触发、不频繁打扰）** | **模拟20条含情绪词/重复行为的对话，人工评判干预时机是否合理** |
| **弃坑率降低（预估）** | **较无AI辅助场景降低40%** | **基于用户行为模拟测试（连续3天完成率对比）** |

> **成本计算说明**：纯大模型方案每次对话需调用 GPT-4o 处理视觉+推理；本方案中图谱查询、RAG检索、CLIP对比均为本地计算，仅视觉解析和兜底对话调用大模型，预计 token 消耗降低 60%。


## 十、数据安全与隐私

- 用户上传的图片仅在内存中处理，**不持久化存储**
- 处理完成后图片缓存在 Redis 中（TTL=30分钟），过期自动清除
- 不收集用户身份信息，仅用会话ID区分对话
- 所有外部API调用（Qwen-VL、飞书Webhook）通过环境变量读取密钥，不硬编码


## 十一、扩展性设计

- **新增套装**：提供 `import_set.py` 脚本，输入套装编号自动从 Rebrickable 拉取 BOM → Neo4j
- **新增零件替代关系**：CSV 批量导入，格式统一为 `[part_a, part_b, confidence, source]`
- **模型可替换**：视觉模型抽象为 `VisionProvider` 接口，Qwen-VL / GPT-4o 可切换
- **话术库可扩展**：`encouragement_library.json` 支持热更新，无需重启服务即可追加新话术


## 十二、简历项目描述（可直接使用）

> **LEGO-Mate —— 基于多模态ReAct Agent的乐高拼搭智能副驾**
>
> 构建LangGraph对话中枢，实现意图自主路由与10轮上下文记忆，工具调用准确率95%+；设计Neo4j知识图谱推理引擎，存储500+零件替代关系，缺件替代100%准确（零幻觉）；集成Qwen-VL多模态解析与CLIP验真闭环，端到端响应<2.5秒。**创新引入挫折感知（Frustration-Aware）激励引擎**，通过行为时序分析（重试频次/挂起时长/情绪词）动态调节交互策略，主动推送共情话术与进度可视化，预估将用户弃坑率降低40%。通过Human-in-the-loop机制保障关键操作安全可控。